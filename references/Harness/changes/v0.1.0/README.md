# v0.1.0 Changes

This directory stores real AgentPM change records for the current `v0.1.0` development line.

Use it for:

- lightweight changes that only need `change.md`
- full-closure changes that need the complete Harness artifact set

Current records:

- `TECH-001-harness-governance-defaults`
- `TECH-002-product-doc-naming`
- `TECH-003-prd-boundary-rule`

Use the current records as examples of lightweight governance work that updates repository rules without changing runtime application behavior.

Do not add new work directly under `v-template/`. Copy from the template into a real change directory here first.
