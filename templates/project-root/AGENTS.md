# AGENTS.md

## Project Goal

<一句话说明本项目要交付的真实业务价值。>

## Current Phase

当前阶段为 `<phase-name>`：

- <当前已有资产，例如 PRD、原型、代码骨架、已上线版本等>
- <当前阶段目标，例如 prototype-to-code、MVP、production-hardening>
- <本阶段最重要的交付闭环>

## Source of Truth

在真实代码和正式文档尚未完全收敛前，以下材料作为设计基线：

- `docs/product/`
- `docs/architecture/`
- `docs/api/`
- `<Obsidian 项目路径>/`

## Engineering Rules

- 默认沿项目声明的前端主线和后端主线推进；legacy fallback 目录必须显式声明，否则不得作为新能力默认落点。
- 需求定义默认以用户故事为主、以原型页为辅；页面用于说明界面与交互，不作为默认需求切分单位。
- 默认优先实现真实端到端闭环、真实状态流转和可保存的业务结果，不把“仅可演示”作为主要交付标准。
- Mock 数据只允许用于未完成接口、复杂外部依赖或高风险能力的短期过渡，不得长期作为业务真相来源。
- demo seed / fixture 只作为开发与测试环境的真实数据承载方式，不应替代正式接口契约、权限校验和持久化边界设计。
- 不要在没有依据时发明新业务规则。
- 对尚未确认的后端字段、状态流转、接口契约，用显式占位、契约文档和注释标记，不得把猜测逻辑直接固化为正式实现。
- 新代码默认遵循 `AGENT/ENGINEERING_RULES.md`、`AGENT/ARCHITECTURE_RULES.md` 与 `references/Harness/rules/`。
- 结构迁移必须小步进行，优先以单个用户故事、单条主链路或单个业务域切片推进，禁止无验证口径的一次性大重构。

## Development Principles

- 先规划后开发：任何代码修改前，先明确需求、边界、影响和验证方式。
- 先输出设计方案：涉及业务、流程、接口或结构调整时，先形成可评审的设计或变更说明，再进入实现。
- 未确认方案不得写代码：接口字段、状态流转、页面行为、数据持久化未确认时，只允许做显式占位、契约说明或受控 Mock。
- 小步迭代：优先拆成单个用户故事、单条主链路或单个可验收切片推进。
- 每个功能必须可测试：交付必须能用测试、冒烟或明确操作步骤验证。
- 每次修改必须更新文档：凡是影响需求、流程、结构、计划或交付结论的改动，都要同步更新对应文档与 `PLANS.md`。

## Development Flow

标准开发顺序为：

`Plan` -> `Review` -> `Grooming` -> `Design` -> `Coding` -> `Testing` -> `Documentation`

## Forbidden Actions

- 禁止直接重构整个系统，必须先拆分范围并分阶段推进。
- 禁止一次性修改多个无关模块，除非它们属于同一个已确认闭环且已拆分验证。
- 禁止删除已有功能，除非有明确替代方案、迁移路径和回退说明。

## Documentation Sync

- 所有设计、架构、流程和需求变更，必须同步到 `docs/`。
- 若项目使用 Obsidian，主工作区为 `<Obsidian 项目路径>/`；相关设计内容应同步到该工作区对应材料中。
- 任何影响执行状态的变更，都要同步更新 `PLANS.md`。

## Governance Entry

- 仓库级业务目标与阶段约束以本文件为准。
- 通用 agent 规则位于 `AGENT/`。
- 详细工程流程、角色分工、执行技能和变更目录模板位于 `references/Harness/`。
- `PLANS.md` 是当前阶段的执行看板；任务完成、阶段判断变化或需求更新且用户要求更新计划时，必须同步更新。

## Current Delivery Scope

当前阶段优先完成以下可用系统主链：

1. <用户故事或业务闭环 1>
2. <用户故事或业务闭环 2>
3. <用户故事或业务闭环 3>

## Non-Goals

- <本阶段不做的范围 1>
- <本阶段不做的范围 2>
- <本阶段不做的范围 3>

## Done Definition

一个开发任务完成时，至少满足：

- 至少交付一个可真实使用的用户故事闭环或清晰的可用故事切片。
- 与原型、需求或业务流程关系清晰。
- 主路径、关键状态、异常路径和主要操作可运行。
- 核心数据可被查询、保存，并在刷新或重新进入后保持一致。
- 关键操作具备基础权限控制、参数校验和失败提示。
- 本地开发环境可以完整运行，而不只是静态预览。
- 后续扩展数据库、权限、外部系统和部署方案的边界清楚。
